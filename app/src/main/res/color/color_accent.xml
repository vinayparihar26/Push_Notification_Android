<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:color="@color/forest_green" android:state_pressed="true" />

    <!-- Focused state -->
    <item android:color="@color/sunset_orange" android:state_focused="true" />

    <!-- Default state -->
    <item android:color="@color/royal_purple" />
</selector>